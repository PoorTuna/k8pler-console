apiVersion: v1
kind: Service
metadata:
  name: {{ include "k8pler-console.fullname" . }}
  labels:
    {{- include "k8pler-console.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "k8pler-console.selectorLabels" . | nindent 4 }}
