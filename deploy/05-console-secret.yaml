# Must match the `secret` field of the "console" staticClient entry in
# deploy/01-dex-configmap.yaml.
apiVersion: v1
kind: Secret
metadata:
  name: console-oidc-client
  namespace: k8pler-console
type: Opaque
stringData:
  client-secret: REPLACE_WITH_A_GENERATED_SECRET
